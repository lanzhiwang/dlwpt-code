# torchrun (Elastic Launch)
torchrun(弹性启动)

* https://docs.pytorch.org/docs/2.13/elastic/run.html

Created On: May 12, 2026 | Last Updated On: May 12, 2026
创建日期: 2026年5月12日 | 最后更新日期: 2026年5月12日

Module `torch.distributed.run`.

`torch.distributed.run` is a module that spawns up multiple distributed training processes on each of the training nodes.
`torch.distributed.run` 是一个模块, 它会在每个训练节点上启动多个分布式训练进程.

`torchrun` is a python [console script](https://packaging.python.org/en/latest/specifications/entry-points/#use-for-scripts) to the main module [torch.distributed.run](https://github.com/pytorch/pytorch/blob/master/torch/distributed/run.py) declared in the `entry_points` configuration in [setup.py](https://github.com/pytorch/pytorch/blob/master/setup.py). It is equivalent to invoking `python -m torch.distributed.run`.
`torchrun` 是一个 Python 程序. [控制台脚本]() 到主模块 [torch.distributed.run]() 在 `entry_points` 配置中声明 [setup.py](). 它相当于调用 `python -m torch.distributed.run`.

`torchrun` can be used for single-node distributed training, in which one or more processes per node will be spawned. It can be used for either CPU training or GPU training. If it is used for GPU training, each distributed process will be operating on a single GPU. This can achieve well-improved single-node training performance. `torchrun` can also be used in multi-node distributed training, by spawning up multiple processes on each node for well-improved multi-node distributed training performance as well. This will especially be beneficial for systems with multiple Infiniband interfaces that have direct-GPU support, since all of them can be utilized for aggregated communication bandwidth.
`torchrun` 可用于单节点分布式训练, 每个节点会生成一个或多个进程. 它既可用于 CPU 训练 `torchrun` 也可用于 GPU 训练. 如果用于 GPU 训练, 每个分布式进程都将在单个 GPU 上运行. 这可以显著提高单节点训练性能. torchrun 也可用于多节点分布式训练, 在每个节点上生成多个进程, 从而同样显著提高多节点分布式训练性能. 这对于具有多个支持直接 GPU 的 Infiniband 接口的系统尤其有利, 因为所有接口都可以用于聚合通信带宽.

In both cases of single-node distributed training or multi-node distributed training, `torchrun` will launch the given number of processes per node (`--nproc-per-node`). If used for GPU training, this number needs to be less or equal to the number of GPUs on the current system (`nproc_per_node`), and each process will be operating on a single GPU from *GPU 0 to GPU (nproc_per_node - 1)*.
无论是单节点分布式训练还是多节点分布式训练, `torchrun` 都会为每个节点启动指定数量的进程( `--nproc-per-node` ). 如果用于 GPU 训练, 则该数量必须小于或等于当前系统上的 GPU 数量( `nproc_per_node` ), 并且每个进程将在 GPU 0 到 GPU (nproc_per_node - 1) 之间的单个 GPU 上运行.

Changed in version 2.0.0: `torchrun` will pass the `--local-rank=<rank>` argument to your script. From PyTorch 2.0.0 onwards, the dashed `--local-rank` is preferred over the previously used underscored `--local_rank`.
版本 2.0.0 中的更改: `torchrun` 会将 `--local-rank=<rank>` 参数传递给您的脚本. 从 PyTorch 2.0.0 开始, 优先使用带短横线的 `--local-rank` 参数, 而不是之前使用的带下划线的 `--local_rank`.

For backward compatibility, it may be necessary for users to handle both cases in their argument parsing code. This means including both `"--local-rank"` and `"--local_rank"` in the argument parser. If only `"--local_rank"` is provided, `torchrun` will trigger an error: "error: unrecognized arguments: –local-rank=<rank>". For training code that only supports PyTorch 2.0.0+, including `"--local-rank"` should be sufficient.
为了向后兼容, 用户可能需要在参数解析代码中处理这两种情况. 这意味着需要同时包含 `"--local-rank"` 参数. 参数解析器中需要包含 `"--local_rank"` 参数. 如果只提供 `"--local_rank"`, `torchrun` 将触发错误: "error: unrecognized arguments: –local-rank=". 对于仅支持 PyTorch 2.0.0 及更高版本的训练代码, 包含 `"--local-rank"` 参数即可.

```python
import argparse
parser = argparse.ArgumentParser()
parser.add_argument("--local-rank", "--local_rank", type=int)
args = parser.parse_args()
```

## Usage
用法

### Single-node multi-worker
单节点多工作进程

```bash
torchrun
 --standalone
 --nnodes=1
 --nproc-per-node=$NUM_TRAINERS
 YOUR_TRAINING_SCRIPT.py (--arg1... train script args...)
```

> Note
> `--nproc-per-node` may be `"gpu"` (spawn one process per GPU), `"cpu"` (spawn one process per CPU), `"xpu"` (spawn one process per XPU), `"auto"` (equivalent to `"gpu"` if CUDA is available, else equivalent to `"xpu"` if XPU is available, else equivalent to `"cpu"`), or an integer specifying the number of processes. See [torch.distributed.run.determine_local_world_size](https://github.com/pytorch/pytorch/blob/0a94bb432ed75cc2d950d81b2921363218a7e459/torch/distributed/run.py#L673-L716) for more details.
> `--nproc-per-node` 可能 `"gpu"` (每个 GPU 生成一个进程), `"cpu"` (每个 CPU 生成一个进程), `"xpu"` (每个 XPU 生成一个进程), `"auto"` (如果 CUDA 可用, 则等效于 `"gpu"` 如果 XPU 可用, 则等效于 `"xpu"` 否则等效于 `"cpu"`), 或指定进程数的整数. 参见 [torch.distributed.run.determine_local_world_size]() 更多详情请见下文.
>

### Stacked single-node multi-worker
堆叠式单节点多工作节点

To run multiple instances (separate jobs) of single-node, multi-worker on the same host, we need to make sure that each instance (job) is setup on different ports to avoid port conflicts (or worse, two jobs being merged as a single job). To do this you have to run with `--rdzv-backend=c10d` and specify a different port by setting `--rdzv-endpoint=localhost:$PORT_k`. For `--nodes=1`, its often convenient to let `torchrun` pick a free random port automatically instead of manually assigning different ports for each run.
要在同一主机上运行多个单节点多工作进程实例(独立作业), 我们需要确保每个实例(作业)都配置在不同的端口上, 以避免端口冲突(或者更糟糕的情况, 两个作业被合并成一个作业). 为此, 您需要使用 `--rdzv-backend=c10d` 参数运行命令. 并通过设置 `--rdzv-endpoint=localhost:$PORT_k` 来指定不同的端口. 对于 `--nodes=1`, 通常让 `torchrun` 自动选择一个空闲的随机端口比每次运行手动分配不同的端口更方便.

```bash
torchrun
 --rdzv-backend=c10d
 --rdzv-endpoint=localhost:0
 --nnodes=1
 --nproc-per-node=$NUM_TRAINERS
 YOUR_TRAINING_SCRIPT.py (--arg1... train script args...)
```

### Fault tolerant (fixed sized number of workers, no elasticity, tolerates 3 failures)
容错型(固定数量的工作节点, 无弹性, 可容忍 3 次故障)

```bash
torchrun
 --nnodes=$NUM_NODES
 --nproc-per-node=$NUM_TRAINERS
 --max-restarts=3
 --rdzv-id=$JOB_ID
 --rdzv-backend=c10d
 --rdzv-endpoint=$HOST_NODE_ADDR
 YOUR_TRAINING_SCRIPT.py (--arg1... train script args...)
```

`HOST_NODE_ADDR`, in form <host>[:<port>] (e.g. node1.example.com:29400), specifies the node and the port on which the C10d rendezvous backend should be instantiated and hosted. It can be any node in your training cluster, but ideally you should pick a node that has a high bandwidth.
`HOST_NODE_ADDR`, 格式为 [:](例如 node1.example.com:29400), 指定 C10d 汇聚后端实例化和托管的节点和端口. 它可以是训练集群中的任何节点, 但理想情况下, 您应该选择一个带宽较高的节点.

> Note
> If no port number is specified `HOST_NODE_ADDR` defaults to 29400.
> 如果没有指定端口号, 则 `HOST_NODE_ADDR` 默认为 29400.
>

### Elastic (`min=1`, `max=4`, tolerates up to 3 membership changes or failures)
弹性( `min=1`, `max=4`, 最多容忍 3 次成员变更或失败)

```bash
torchrun
 --nnodes=1:4
 --nproc-per-node=$NUM_TRAINERS
 --max-restarts=3
 --rdzv-id=$JOB_ID
 --rdzv-backend=c10d
 --rdzv-endpoint=$HOST_NODE_ADDR
 YOUR_TRAINING_SCRIPT.py (--arg1... train script args...)
```

`HOST_NODE_ADDR`, in form <host>[:<port>] (e.g. node1.example.com:29400), specifies the node and the port on which the C10d rendezvous backend should be instantiated and hosted. It can be any node in your training cluster, but ideally you should pick a node that has a high bandwidth.
`HOST_NODE_ADDR`, 格式为 [:](例如 node1.example.com:29400), 指定 C10d 汇聚后端实例化和托管的节点和端口. 它可以是训练集群中的任何节点, 但理想情况下, 您应该选择一个带宽较高的节点.

> Note
> If no port number is specified `HOST_NODE_ADDR` defaults to 29400.
> 如果没有指定端口号, 则 `HOST_NODE_ADDR` 默认为 29400.
>

## Note on rendezvous backend
关于会合后端的说明

For multi-node training you need to specify:
对于多节点训练, 您需要指定:

1. `--rdzv-id`: A unique job id (shared by all nodes participating in the job)
  `--rdzv-id`: 唯一的作业 ID(参与该作业的所有节点共享此 ID)

2. `--rdzv-backend`: An implementation of [`torch.distributed.elastic.rendezvous.RendezvousHandler`](https://docs.pytorch.org/docs/2.13/elastic/rendezvous.html#torch.distributed.elastic.rendezvous.RendezvousHandler "torch.distributed.elastic.rendezvous.RendezvousHandler")
  `--rdzv-backend`: 一种实现 [torch.distributed.elastic.rendezvous.RendezvousHandler]()

3. `--rdzv-endpoint`: The endpoint where the rendezvous backend is running; usually in form `host:port`.
  `--rdzv-endpoint`: Rendezvous 后端运行的端点; 通常以这样的形式出现: `host:port`.

Currently `c10d` (recommended), `etcd-v2`, and `etcd` (legacy) rendezvous backends are supported out of the box. To use `etcd-v2` or `etcd`, setup an etcd server with the `v2` api enabled (e.g. `--enable-v2`).
目前, `c10d` (推荐)、`etcd-v2` 和 `etcd` (旧版)rendezvous 后端均已开箱即用地支持. 要使用 `etcd-v2` 或 `etcd`, 请设置一个启用 `v2` API 的 etcd 服务器(例如, 使用 `--enable-v2` 参数).

> Warning
> `etcd-v2` and `etcd` rendezvous use etcd API v2. You MUST enable the v2 API on the etcd server. Our tests use etcd v3.4.3.
> `etcd-v2` 和 `etcd` rendezvous 使用 etcd API v2. 您必须在 etcd 服务器上启用 v2 API. 我们的测试使用 etcd v3.4.3.
>

> Warning
> For etcd-based rendezvous we recommend using `etcd-v2` over `etcd` which is functionally equivalent, but uses a revised implementation. `etcd` is in maintenance mode and will be removed in a future version.
> 对于基于 etcd 的 Rendezvous 服务, 我们建议使用 `etcd-v2` 而非 `etcd`, 两者功能相同, 但 `etcd` 使用了改进的实现. etcd 目前处于维护模式, 将在未来的版本中移除.
>

## Definitions

1. `Node` - A physical instance or a container; maps to the unit that the job manager works with.
  `Node` - 物理实例或容器; 映射到作业管理器所操作的单元.

2. `Worker` - A worker in the context of distributed training.
  `Worker` - 分布式培训中的工作人员.

3. `WorkerGroup` - The set of workers that execute the same function (e.g. trainers).
  `WorkerGroup` - 执行相同功能的工人集合(例如, 训练员).

4. `LocalWorkerGroup` - A subset of the workers in the worker group running on the same node.
  `LocalWorkerGroup` - 在同一节点上运行的工作组中的工作线程的子集.

5. `RANK` - The rank of the worker within a worker group.
  `RANK` - 指工人在工人群体中的等级.

6. `WORLD_SIZE` - The total number of workers in a worker group.
  `WORLD_SIZE` - 工作组中的工作者总数.

7. `LOCAL_RANK` - The rank of the worker within a local worker group.
  `LOCAL_RANK` - 本地工作组中工作进程的排名.

8. `LOCAL_WORLD_SIZE` - The size of the local worker group.
  `LOCAL_WORLD_SIZE` - 本地工作组的大小.

9. `rdzv_id` - A user-defined id that uniquely identifies the worker group for a job. This id is used by each node to join as a member of a particular worker group.
  `rdzv_id` - 用户自定义的 ID, 用于唯一标识作业所属的工作组. 每个节点都使用此 ID 加入特定的工作组.

9. `rdzv_backend` - The backend of the rendezvous (e.g. `c10d`). This is typically a strongly consistent key-value store.
  `rdzv_backend` - 会合点的后端(例如 `c10d` ). 这通常是一个强一致性的键值存储.

10. `rdzv_endpoint` - The rendezvous backend endpoint; usually in form `<host>:<port>`.
  `rdzv_endpoint` - 会合后端端点; 通常采用 `<host>:<port>` 的形式.

A `Node` runs `LOCAL_WORLD_SIZE` workers which comprise a `LocalWorkerGroup`. The union of all `LocalWorkerGroups` in the nodes in the job comprise the `WorkerGroup`.
一个 `Node` 运行 `LOCAL_WORLD_SIZE` 个工作进程, 这些工作进程组成一个 `LocalWorkerGroup`. 作业中所有节点的 `LocalWorkerGroups` 的并集构成 `WorkerGroup`.

## Environment Variables

The following environment variables are made available to you in your script:
以下环境变量可供您在脚本中使用:

1. `LOCAL_RANK` - The local rank.
  `LOCAL_RANK` - 本地排名.

2. `RANK` - The global rank.
  `RANK` - 全球排名.

3. `GROUP_RANK` - The rank of the worker group. A number between 0 and `max_nnodes`. When running a single worker group per node, this is the rank of the node.
  `GROUP_RANK` - 工作组的排名. 取值范围为 0 到 `max_nnodes` 之间的数字. 当每个节点只运行一个工作组时, 此值即为该节点的排名.

4. `ROLE_RANK` - The rank of the worker across all the workers that have the same role. The role of the worker is specified in the `WorkerSpec`.
  `ROLE_RANK` - 该工作进程在所有具有相同角色的工作进程中的排名. 工作进程的角色在 `WorkerSpec` 中指定.

5. `LOCAL_WORLD_SIZE` - The local world size (e.g. number of workers running locally); equals to `--nproc-per-node` specified on `torchrun`.
  `LOCAL_WORLD_SIZE` - 本地世界大小(例如, 本地运行的工作进程数); 等于 在 `torchrun` 上指定 `--nproc-per-node`.

6. `WORLD_SIZE` - The world size (total number of workers in the job).
  `WORLD_SIZE` - 世界规模(工作中的工人总数).

7. `ROLE_WORLD_SIZE` - The total number of workers that was launched with the same role specified in `WorkerSpec`.
  `ROLE_WORLD_SIZE` - 使用 `WorkerSpec` 中指定的相同角色启动的工作进程总数.

8. `MASTER_ADDR` - The FQDN of the host that is running worker with rank 0; used to initialize the Torch Distributed backend.
  `MASTER_ADDR` - 运行 rank 为 0 的工作进程的主机的 FQDN; 用于初始化 Torch 分布式后端.

9. `MASTER_PORT` - The port on the `MASTER_ADDR` that can be used to host the C10d TCP store.
  `MASTER_PORT` - `MASTER_ADDR` 上可用于托管 C10d TCP 存储的端口.

10. `TORCHELASTIC_RESTART_COUNT` - The number of worker group restarts so far.
  `TORCHELASTIC_RESTART_COUNT` - 到目前为止工作组重启的次数.

11. `TORCHELASTIC_MAX_RESTARTS` - The configured maximum number of restarts.
  `TORCHELASTIC_MAX_RESTARTS` - 配置的最大重启次数.

12. `TORCHELASTIC_RUN_ID` - Equal to the rendezvous `run_id` (e.g. unique job id).
  `TORCHELASTIC_RUN_ID` - 等于 rendezvous `run_id` (例如, 唯一作业 ID).

13. `PYTHON_EXEC` - System executable override. If provided, the python user script will use the value of `PYTHON_EXEC` as executable. The sys.executable is used by default.
  `PYTHON_EXEC` - 系统可执行文件覆盖. 如果提供此设置, Python 用户脚本将使用 `PYTHON_EXEC` 的值作为可执行文件. 默认情况下使用 sys.executable.

## Deployment

1. (Not needed for the C10d backend) Start the rendezvous backend server and get the endpoint (to be passed as `--rdzv-endpoint` to `torchrun`)
  (C10d 后端不需要此步骤)启动 Rendezvous 后端服务器并获取端点(作为 `--rdzv-endpoint` 传递给 `torchrun` ).

2. Single-node multi-worker: Start `torchrun` on the host to start the agent process which creates and monitors a local worker group.
  单节点多工作进程: 在主机上启动 `torchrun` 以启动代理进程, 该代理进程会创建并监控本地工作组.

3. Multi-node multi-worker: Start `torchrun` with the same arguments on all the nodes participating in training.
  多节点多工作节点: 在参与训练的所有节点上使用相同的参数启动 `torchrun`.

When using a job/cluster manager, the entry point command to the multi-node job should be `torchrun`.
使用作业/集群管理器时, 多节点作业的入口点命令应该是 `torchrun`.

## Failure Modes
故障模式

1. Worker failure: For a training job with `n` workers, if `k<=n` workers fail all workers are stopped and restarted up to `max_restarts`.
  工作进程故障: 对于一个有 `n` 工作进程的训练作业, 如果 `k<=n` 工作进程发生故障, 则所有工作进程都会停止并重新启动, 最多可以启动 `max_restarts` 次.

2. Agent failure: An agent failure results in a local worker group failure. It is up to the job manager to fail the entire job (gang semantics) or attempt to replace the node. Both behaviors are supported by the agent.
  代理故障: 代理故障会导致本地工作组故障. 作业管理器可以决定是让整个作业失败(组语义)还是尝试替换故障节点. 代理支持这两种行为.

3. Node failure: Same as agent failure.
  节点故障: 与代理故障相同.

## Membership Changes
成员变更

1. Node departure (scale-down): The agent is notified of the departure, all existing workers are stopped, a new `WorkerGroup` is formed, and all workers are started with a new `RANK` and `WORLD_SIZE`.
  节点退出(缩减规模): 代理收到节点退出的通知, 所有现有工作节点停止运行, 创建一个新的 `WorkerGroup`, 所有工作节点以新的 `RANK` 和 1 启动. `WORLD_SIZE`.

2. Node arrival (scale-up): The new node is admitted to the job, all existing workers are stopped, a new `WorkerGroup` is formed, and all workers are started with a new `RANK` and `WORLD_SIZE`.
  节点到达(扩容): 新节点加入作业, 所有现有工作节点停止, 创建一个新的 `WorkerGroup`, 所有工作节点以新的 `RANK` 和 100% 启动. `WORLD_SIZE`.

## NUMA Binding
NUMA 绑定

On multi-GPU systems with NUMA (Non-Uniform Memory Access) architecture, you can improve performance by binding worker processes to CPUs near their assigned GPUs. Use the `--numa-binding` flag:
在采用 NUMA(非统一内存访问)架构的多 GPU 系统中, 您可以提高性能. 通过将工作进程绑定到与其分配的 GPU 距离最近的 CPU 来提高性能. 使用 `--numa-binding` 标志:

```bash
torchrun --numa-binding=node --nproc-per-node=8 train.py
```

See [NUMA Binding](https://docs.pytorch.org/docs/2.13/elastic/numa.html#numa-api) for more details.
请参阅 [NUMA 绑定]()了解更多详情.

## Important Notices
重要通知

1. This utility and multi-process distributed (single-node or multi-node) GPU training currently only achieves the best performance using the NCCL distributed backend. Thus NCCL backend is the recommended backend to use for GPU training.
  目前, 该实用程序和多进程分布式(单节点或多节点)GPU 训练仅在使用 NCCL 分布式后端时才能达到最佳性能. 因此, NCCL 后端是推荐用于 GPU 训练的后端.

2. The environment variables necessary to initialize a Torch process group are provided to you by this module, no need for you to pass `RANK` manually. To initialize a process group in your training script, simply run:
  本模块已提供初始化 Torch 进程组所需的环境变量, 您无需手动传递 `RANK`. 要在训练脚本中初始化进程组, 只需运行以下命令:

```python
import torch.distributed as dist
dist.init_process_group(backend="gloo|nccl")
```

3. In your training program, you can either use regular distributed functions or use [`torch.nn.parallel.DistributedDataParallel()`](https://docs.pytorch.org/docs/2.13/generated/torch.nn.parallel.DistributedDataParallel.html#torch.nn.parallel.DistributedDataParallel "torch.nn.parallel.DistributedDataParallel") module. If your training program uses GPUs for training and you would like to use [`torch.nn.parallel.DistributedDataParallel()`](https://docs.pytorch.org/docs/2.13/generated/torch.nn.parallel.DistributedDataParallel.html#torch.nn.parallel.DistributedDataParallel "torch.nn.parallel.DistributedDataParallel") module, here is how to configure it.
  在你的训练程序中, 你可以使用常规的分布式函数, 也可以使用 [torch.nn.parallel.DistributedDataParallel()]() 模块. 如果你的训练程序使用 GPU 进行训练, 而您希望使用 [torch.nn.parallel.DistributedDataParallel()]() 模块, 以下是配置方法.

```python
local_rank = int(os.environ["LOCAL_RANK"])
model = torch.nn.parallel.DistributedDataParallel(
    model, device_ids=[local_rank], output_device=local_rank
)
```

Please ensure that `device_ids` argument is set to be the only GPU device id that your code will be operating on. This is generally the local rank of the process. In other words, the `device_ids` needs to be `[int(os.environ("LOCAL_RANK"))]`, and `output_device` needs to be `int(os.environ("LOCAL_RANK"))` in order to use this utility
请确保 `device_ids` 参数设置为代码将要操作的唯一 GPU 设备 ID. 这通常是进程的本地 Rank. 换句话说, 为了使用此实用程序, `device_ids` 需要设置为 `[int(os.environ("LOCAL_RANK"))]`, 并且 `output_device` 也需要设置 `int(os.environ("LOCAL_RANK"))`

4. On failures or membership changes ALL surviving workers are killed immediately. Make sure to checkpoint your progress. The frequency of checkpoints should depend on your job's tolerance for lost work.
  任务失败或成员变更时, 所有幸存的工人都会立即死亡. 请务必定期检查进度. 检查频率应取决于任务对工作损失的容忍度.

5. This module only supports homogeneous `LOCAL_WORLD_SIZE`. That is, it is assumed that all nodes run the same number of local workers (per role).
  本模块仅支持同构的 `LOCAL_WORLD_SIZE`. 也就是说, 假定所有节点运行相同数量的本地工作进程(每个角色).

6. `RANK` is NOT stable. Between restarts, the local workers on a node can be assigned a different range of ranks than before. NEVER hard code any assumptions about the stable-ness of ranks or some correlation between `RANK` and `LOCAL_RANK`.
  `RANK` 并不稳定. 在重启之间, 节点上的本地工作节点可能会被分配到与之前不同的 RANK 值范围. 切勿对 RANK 值的稳定性或 `RANK` 与 `LOCAL_RANK` 之间的任何相关性做出任何硬编码假设.

7. When using elasticity (`min_size!=max_size`) DO NOT hard code assumptions about `WORLD_SIZE` as the world size can change as nodes are allowed to leave and join.
  使用弹性( `min_size!=max_size` )时, 请勿硬编码任何假设. `WORLD_SIZE` 表示世界大小会随着节点的离开和加入而改变.

8. It is recommended for your script to have the following structure:
  建议您的脚本采用以下结构:

```python
def main():
    load_checkpoint(checkpoint_path)
    initialize()
    train()


def train():
    for batch in iter(dataset):
        train_step(batch)

        if should_checkpoint:
            save_checkpoint(checkpoint_path)
```

9. (Recommended) On worker errors, this tool will summarize the details of the error (e.g. time, rank, host, pid, traceback, etc). On each node, the first error (by timestamp) is heuristically reported as the "Root Cause" error. To get tracebacks as part of this error summary print out, you must decorate your main entrypoint function in your training script as shown in the example below. If not decorated, then the summary will not include the traceback of the exception and will only contain the exitcode. For details on torchelastic error handling see: https://pytorch.org/docs/stable/elastic/errors.html
  (推荐)对于工作节点错误, 此工具将汇总错误详情(例如时间、排名、主机、进程 ID、回溯信息等). 在每个节点上, 第一个错误(按时间戳排序)会被启发式地报告为"根本原因"错误. 要在此错误摘要输出中包含回溯信息, 您必须按照以下示例所示, 在训练脚本中装饰您的主入口点函数. 如果未进行装饰, 则摘要将不包含异常回溯信息, 而仅包含退出代码. 有关 torchelastic 错误处理的详细信息, 请参阅: https://pytorch.org/docs/stable/elastic/errors.html
